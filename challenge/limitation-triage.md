# Design-Time Limitation Triage (Step 6, round 2)

**Run**: 2026-08-03-followup-follow-up-spectral-optimizer-redo-pxp-filter-walk-
**Inputs**: challenge/assumption-analysis.md (round 2, "AA"), challenge/mentor-review.md (round 2, "MR", verdict MINOR_REVISIONS), challenge/pre-mortem.md (round 2, "PM"). Round-1 files preserved as challenge/round1-*.md.
**Rubric**: Shared Limitation Triage, judged against `compute_profile` (MATS free partition, ≤6 L40s concurrent, ≤5 experiment units) and remaining budget (0 of 5 units spent; EU-1..EU-5 planned, EU-5 is the reserve).

Every fix-now item below is **fix-now-free**: a wording, logging, sim-extension, or job-structuring change with zero GPU cost, legitimate only before the protocol freeze that gates EU-2. None adds an experiment unit — the 5-unit cap is respected; any extra *submission* (EU-1 continuation, fold split) is charged against the EU-5 reserve per the decomposition's existing accounting. The fix-now items are folded into `decomposition.md` as the binding **Round-2 Challenge Addendum (D-series)**; this table is the disposition record.

## Fix-now (all free) — folded into decomposition.md as D1–D13

| # | Limitation | Source | Disposition | Concrete fix (cost: zero compute) |
|---|-----------|--------|-------------|-----------------------------------|
| D1 | A step count fixed by arm A's convergence run may under-train arm B (convergence speed is rank-dependent per H3); none of the A3 signatures detects under-training, so the kill could certify a schedule artifact | AA crit #1 | fix-now-free | Add a 4th under-exploration signature to the A3 list: arm B's selected config still improving at step cutoff (VALID slope over final segment above a pre-registered threshold) → same downgrade path. Log VALID-score-vs-step for every arm-B trial. Verdict claim language calibrated to "at matched step budget". |
| D2 | Arm C sits between two degeneracies: candidate (a) collapses to noise injection (~k/p energy), candidate (b) as written can be a clone of B (projection is basis-invariant within the retained span), pre-baking the mechanism verdict in either direction; the B3 sim's acceptance test polices only the "too far" tail | AA crit #2, PM #5, MR rec 1 | fix-now-free | Pre-designate candidate (b) *with a strictly larger history span* (rotate within an r > k ambient space, then truncate to k) as the expected primary design. Extend the required B3 CPU sim with (i) distinctness criteria vs B (principal-angle floor, per-step update-cosine ceiling) and (ii) a positive-control-for-the-control: an acceptable C must FAIL the #5 planted-subspace task where B succeeds. Pre-register the empty-middle fallback: drop C, scope the run to (B−A), report the sim as evidence no fair control exists, redirect C's refit-seed budget toward 5 seeds. |
| D3 | "MDE ≤ 0.005" licenses a kill at ~50% power at the boundary effect, and 0.005 is ~35% of the gated baseline's own score — a "clean kill" could excuse a true ~20%-relative effect; compounds with possible anti-conservative 3-seed hierarchical CI | AA crit #3 + mod #7, MR rec 2 | fix-now-free | In protocol.json: one-sentence derivation anchoring 0.005 to a decision-relevant effect size; report the quantity as "CI half-width (≈50%-power MDE)"; connect the kill's A1 condition to the B4 sim's P(detect ±0.005) quantity; state the kill bound in relative terms against realized arm A. Add a CI coverage check to the #3 sim at 3 and 5 seeds; materially sub-nominal 3-seed coverage upgrades the 5-seed packing option from power lever to validity lever. |
| D4 | Stage-1 transferred LR (tuned for arm A) systematically handicaps low-rank configs (effective step size shrinks with kept-norm fraction), mis-ranking the grid toward near-identity winners — and the A3 LR-shift signature is silent exactly then; foreseeable self-trigger of A3 by A4 | AA crit #4, PM #3 | fix-now-free | Scale the stage-1 transferred LR per grid point by the EU-1-measured expected kept-norm fraction at that rank (one line of arithmetic per trial), and spend one pre-registered stage-1 trial as a kept-norm-corrected LR probe at the modal rank point (frozen rule: if the probe beats its transferred-LR twin, stage-1 centering is corrected). Treat EU-1's kept-norm diagnostics as a freeze *input*: checklist item 4 (transferred LR) is written as a function of them. Add "stage-1 winner within one grid step of the identity-distance minimum" as a further under-exploration signature. |
| D5 | Refit stopping rule (steps × rows_refit/rows_train) assumes linear-in-rows convergence; a short-stopped refit contaminates both the gate ratio and (B−A) on the largest fold, unfixable post freeze | PM #3 | fix-now-free | Freeze a deterministic refit-extension rule beside the stopping rule: if refit loss slope over the last 5% of steps exceeds a stated threshold, extend once by a stated factor and log it. Also fix cross-fold trial-step semantics with a one-line rule (trial steps scale by rows_train_fold/rows_train_EU1, or explicitly fixed — stated either way). |
| D6 | EU-1 carries ~8 measurement objectives including a convergence run whose duration is the unknown being measured; a `--qos=debug` 2 h truncation starves checklist items 3/4/5/7/11, silently regresses sizing to extrapolation, and eats the reserve scenario 1 needs | PM #2, MR rec 3 | fix-now-free | Restructure EU-1 as ordered, individually persisted phases: (1) shard build → persist; (2) timings/VRAM/eigh variants at both architectures → persist complete timing table; (3) identity re-assert + stability; (4) convergence run LAST, checkpointed every N steps, VALID-score series streamed to NFS, with plateau-detection stop. Pre-authorize the fallback: if phase 4 outgrows the debug window, the continuation (EU-1b) runs on the normal queue with `--time=6h`, resumable, charged to the EU-5 reserve — unit accounting decided now, not mid-flight. Pre-register the truncation anchor: step count entering protocol.json = checkpointed lower bound + stated extrapolation rule, declared as such. |
| D7 | The EU-1 VALID-slice proxy gate ratio is an uncalibrated forecaster of a TEST-block quantity (regime offset); a false pass leaves the reserve unpositioned for the near-miss band | PM #1, AA mod #6 | fix-now-free | Calibrate the proxy before EU-1 is submitted: compute the example model's own VALID-slice vs TEST-block mean per-era corr offset per fold from the existing `out/example_per_era_corr.csv` (free local pandas job) and report the proxy WITH that offset applied. Pre-commit reserve allocation to the calibrated proxy: calibrated ratio < 0.60 → EU-5 provisionally earmarked for the fix ladder before EU-2 launches, and EU-2's phase 2 (arms B/C) made conditional in the sbatch structure. Proxy triggers sizing decisions only — the real gate still runs, fold-1-first. |
| D8 | The amendments bought interpretability, not decisiveness: the qualification triggers are correlated (all descend from the compute envelope) and the modal clean outcome is a multiply-qualified bound — knowable at freeze time from pre-TEST signals | PM #4 | fix-now-free | Pre-register a pooled cross-fold secondary estimand: era-level paired (B−A) series concatenated across gated folds under one hierarchical moving-block bootstrap (~220–330 eras); it cannot overturn the per-fold decision rule but converts three underpowered bounds into one adequately powered one. Add a pre-freeze decisiveness checkpoint to the checklist: if the #3 power sim FAILs its B4 threshold AND EU-1 realizes K_max < 512, take a considered branch — re-scope to 2 folds with proportionally longer TEST blocks (reported), or record in protocol.json the explicit decision to run for a bound ("this design's modal outcome is a bound, not a verdict"). |
| D9 | A3 signature 2 references an across-seed sd that stage 1 (one trial per grid point) never produces — a pre-registered guard whose inputs don't exist gets reinterpreted post hoc; and the stage-1 enumeration (6 points) doesn't match its stated count (7–8) | AA mod #8, bg #13 | fix-now-free | Name signature 2's sd source in protocol.json (across-seed sd of the arm-A refit at the same fold, or one deliberately duplicated stage-1 trial at the H3-prior rank ≈ 32). Reconcile the stage-1 enumeration with the trial count explicitly (the slack trials are the D4 LR probe and the D9 duplicate, if used). |
| D10 | The 0.010 denominator floor creates a mirror problem: a low-signal-flagged fold's role in the ≥2/3 decision rule and the gate-failure tally is unspecified — a fold could fail its gate for regime reasons yet count toward a baseline-construction negative | AA mod #5 | fix-now-free | Component #9 computes all three denominators locally before freeze: if none lands near 0.010, record that and retire the concern; if any does, pre-register the flagged fold's role (full vote / reported-but-excluded / triggers fold substitution) BEFORE the number can motivate the choice. |
| D11 | Arm C's design is frozen from a synthetic sim + short-run diagnostics; if the real basis-rotation rate or effective rank drifts over the full schedule, C drifts off the A7 invariants late in training, degrading the fair-control evidence | AA mod #9 | fix-now-free | Pre-register a post-hoc invariant-match report (realized principal-angle and norm-ratio match between B and C over the full schedule, per fold — all already logged per A8/B3) as the fair-control evidence the symmetric honesty clause consumes. Converts the assumption into a measured quantity. |
| D12 | The kill wording claims "dead for financial tabular regression" from one dataset, one architecture, one p — the one place the frozen criteria overclaim relative to their own compute-feasibility section | AA mod #10 | fix-now-free | Freeze-time wording: "dead for this problem class as instantiated (Numerai v5 tabular regression, MLP, p ≈ 600k, k ≤ K_max)" — extending the A2 scoping move to the dataset/architecture/scale axes. |
| D13 | Specification-fidelity risk: the criteria are now the run's most complex artifact and the Step 10 audit judges clause by clause; a dropped clause under time pressure becomes an audit finding; the decay probe consumes a thin stage-2 budget | MR ("not examined yet") | fix-now-free | protocol.json is the single operational source of truth; every fold job prints which checklist items it satisfies, so fidelity is checkable from logs. Priority rule for stage 2: if stage 1 shows sharp LR sensitivity, the decay=0.999 probe is the trial that gets dropped (the decay limitation is already declared). |

## Future work (beyond this run's compute profile) — carries to Step 11

| # | Limitation | Source | What a fix requires |
|---|-----------|--------|---------------------|
| FW1 | Era-level power ceiling: ~110 autocorrelated eras per fold bounds per-fold MDE regardless of seeds; even a flawless run most likely bounds the effect rather than delivering a terminal verdict | PM #4, residual risk | A decisive test needs more evaluation eras per comparison: longer TEST blocks / more folds / pooled-primary design beyond the 5-unit envelope — same cluster but roughly 2–3× the experiment units and wall-clock (est. 10–15 additional fold-job hours on L40s), or equivalent paid compute. The D8 pooled secondary estimand and the write-up's costed ask are this run's down payment. |
| FW2 | Generality: verdict instantiated on one dataset (Numerai v5), one architecture (MLP), one scale (p ≈ 600k) | AA mod #10 | Additional datasets (other financial tabular regression tasks), a second genuinely-suited architecture, and a p-scaling study — each roughly a full run of this design; multi-dataset sweep plausibly needs faster/paid GPUs or several weeks of free-partition scheduling. |
| FW3 | Rank-grid ceiling: if the cubic (k+1)×(k+1) eigh wall caps realized K_max below 512/2048, the high-rank hypothesis region goes untested (A2 conversion) | PM #4, decomposition #8 | Testing k ≥ 512 at full scale needs the timed eigh-every-N / GPU-fp32 variants validated as faithful, or more GPU-hours per trial than the free-partition budget allows; A100/H100-class hardware (not authorized this run) would relax the wall directly. |
| FW4 | decay/warmup largely untuned: warmup fixed at repo default; decay probed at exactly one alternative (0.999) in stage 2 | A8 declared limitation | A proper filter-hyperparameter study (decay × warmup × alpha grid) ≈ one additional experiment unit per fold at matched budget — deferred; declared limitation stands in the write-up. |

## Accepted risks (no design change; stated plainly)

- **Assumption 3 (gate reachability) is genuinely ~50/50 and only running tests it** (PM residual). The amendments make its failure cheap and informative (bands, ladder, learning-curve conversion), not less likely. A second consecutive baseline-construction report remains a live outcome and is a pre-registered, publishable finding under the round-2 criteria.
- **The calibrated proxy is still a heuristic** — it gates sizing decisions only; the real gate runs fold-1-first (AA mod #6).
- **B4 power-sim input provenance** (parent per-era diffs from the B×B variant on the broken protocol) — self-correcting; the verdict consumes realized per-fold MDEs; provenance noted in the sim output (AA bg #11).
- **Fold non-independence** — handled as claim calibration (A6 nested-fold language), carried from round 1.
- **torch 2.5.1 pin compatibility** — covered by the EU-1 on-cluster identity re-assert, carried from round 1.
- **C1 novelty mini-search** (GaLore-lineage positioning) remains deferred to pre-write-up (Step 11) per the round-1 decision — risk is to positioning, not validity.

## Cap accounting

No fix adds an experiment unit: D1–D13 live inside EU-1's existing phases, the two local CPU sims, the frozen protocol text, and the stage-1 trial budget (the D4 LR probe and optional D9 duplicate are among the 12 trials, per A4's slack). Potential extra *submissions* (EU-1b continuation, >16 h fold splits) were already chargeable to the EU-5 reserve under the decomposition's accounting and remain so. The 5-experiment cap stands.
